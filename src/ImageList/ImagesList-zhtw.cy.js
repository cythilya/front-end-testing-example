import ImageItem from './ImageItem';

describe('ImageList', () => {
  describe('ImageList', () => {
    it('should render no data prompt when no images', () => {
      // 攔截 API https://dummyjson.com/products 的回應，使用 fixtures 資料夾中的 `empty.json`
      // 渲染 `<ImageList />` 元件
      // 斷言沒有資料時會出現 "no-data-prompt" 提示
    });

    it('should render correct image items when have data', () => {
      // 攔截 API https://dummyjson.com/products 的回應，使用 fixtures 資料夾中的 `empty.json`
      // 渲染 `<ImageList />` 元件
      // 斷言 image items length is 2
    });
  });
});
