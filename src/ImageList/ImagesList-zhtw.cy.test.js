import ImageItem from './ImageItem';

describe('ImageList', () => {
  describe('ImageList', () => {
    it('should render no data prompt when no images', () => {
      // Intercept the response from the API https://dummyjson.com/products and use the `empty.json` file in the fixtures folder
      // Render the `<ImageList />` component
      // Assert that the "no-data-prompt" message is displayed when there is no data
    });

    it('should render correct image items when have data', () => {
      // Intercept the response from the API https://dummyjson.com/products and use the `empty.json` file in the fixtures folder
      // Render the `<ImageList />` component
      // Assert that the length of the image items is 2
    });
  });
});